import {combineReducers} from 'redux';

const popupBlock = (state = null, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default combineReducers({
    popupBlock,
});